services:

  redis:
    image: redis
    container_name: cache
    expose:
      - 6379

  backend:
    build: tests/.
    ports:
      - "8000:8000"

  donkey:
    depends_on:
      - backend
      - redis
    build: .
    ports:
      - "3000:3000"
